// const Team = require("./models").team;

// async function getTeamByPk(key) {
//   const team = await Team.findByPk(key);
//   return team ? team.get({ plain: true }) : "Not found!";
// }

// getTeamByPk(2).then((result) => console.log(result));
